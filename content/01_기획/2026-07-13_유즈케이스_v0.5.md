---
title: A2팀 유즈케이스 v0.5
author: lsg
category: 01_기획
status: draft
created: 2026-07-12
updated: 2026-07-13
tags:
  - 유즈케이스
  - v0.5
summary_target: true
---

# A2팀 유즈케이스 v0.5
> 작성일: 2026-07-12 | 수정일: 2026-07-13 | 작성자: 이슬기 (PM)
> **v0.5 진행 중 — 손찬용 PM과 월요일 통합 예정 (최종본 아님, 각자 작성 후 병합)**
> 범위: 핵심 흐름만 다룸 — 로그인·회원가입·프로필 등 정형화된 CRUD 기능은 `2026-07-13_기능명세서_v0.8.md`의 인수 조건으로 대체하고 여기서는 제외
> 기준 문서: IA v0.6 / 기능명세서 v0.8 / 프롬프트가이드 v4 / 마일스톤 v3.2
> v0.5 변경: **관련 문서 참조를 기능명세서 v0.8로 갱신** (섹션 번호 변동 없음) / **UC-VIDEO-01에 오디오 포함 필수 요건 반영**(C팀 전달 조건) / **3x3 그리드 입력 처리 방식 논의 중 각주 추가**(Kling 3.0 BE 테스트에서 그리드 노출·순서 오류 확인, Seedance 2.0 R2V 슬라이스 경로 검토 중 — 미확정) / 모델 선택 근거표에 오디오 반영 시 비용 격차 축소 각주 추가

**관련 문서**
- 화면 구조 → `2026-07-12_IA_v0.6.md`
- 기능 상세·인수 조건 → `2026-07-13_기능명세서_v0.8.md`
- 프롬프트 구조 → `2026-07-10_프롬프트가이드_v4.md`
- 일정·등급 조건 → `framewave-docs/2026-07-06_A2팀_최종_마일스톤_v3.2.md`

**형식**

| 항목 | 내용 |
|------|------|
| 유즈케이스명 | |
| 유즈케이스 ID | |
| 주 액터 | |
| 목표 | |
| 사전조건 | |
| 사후조건 | |
| 우선순위 | |
| 관련 문서 | |

기본 흐름·대안/예외 흐름은 표 아래에 목록으로 별도 기술.

---

## 목차
1. [UC-IMAGE-01. 메뉴 A — 초기 레퍼런스 생성](#uc-image-01-메뉴-a--초기-레퍼런스-생성)
2. [UC-IMAGE-02. 메뉴 B — 키이미지(캐릭터 시트) 생성](#uc-image-02-메뉴-b--키이미지캐릭터-시트-생성)
3. [UC-PROMPT-01. AI 프롬프트 변환 토글 사용](#uc-prompt-01-ai-프롬프트-변환-토글-사용)
4. [UC-VIDEO-01. 영상 생성](#uc-video-01-영상-생성)
5. [UC-VIDEO-02. 영상 재생성](#uc-video-02-영상-재생성)
6. [UC-LIBRARY-01. 라이브러리 조회·필터링](#uc-library-01-라이브러리-조회필터링)
7. [UC-LIBRARY-02. 라이브러리 상세보기·다운로드·삭제](#uc-library-02-라이브러리-상세보기다운로드삭제)
8. [UC-LIBRARY-03. 찜하기(즐겨찾기) 토글](#uc-library-03-찜하기즐겨찾기-토글)
9. [UC-REVERSE-01. 역프롬프트 — 업로드→분석→수정→재생성](#uc-reverse-01-역프롬프트--업로드분석수정재생성)

---

## UC-IMAGE-01. 메뉴 A — 초기 레퍼런스 생성

| 항목 | 내용 |
|------|------|
| 유즈케이스명 | 메뉴 A — 초기 레퍼런스 생성 |
| 유즈케이스 ID | UC-IMAGE-01 |
| 주 액터 | 방송 PD/AD — 방송 프로그램 기획·제작 실무자. 포토샵 등 전문 디자인 툴이나 프롬프트 엔지니어링에 익숙하지 않음. 여러 프로그램을 동시에 맡는 경우가 많아 결과물을 하나하나 다듬기보다 촬영 전 짧은 시간 안에 캐릭터·배경의 대략적인 방향을 빠르게 잡아보고 싶어 함 |
| 목표 | 간단한 한 줄 프롬프트로 캐릭터·배경의 초기 형태를 빠르게 확인한다 |
| 사전조건 | 로그인 상태일 것 / 이미지 생성 화면(메뉴 A)에 진입해 있을 것 |
| 사후조건 | 배경 1장 또는 캐릭터 다각도 이미지가 결과 영역에 표시되고, 생성과 동시에 라이브러리에 자동 저장됨 |
| 우선순위 | P0 (B등급 필수 — M001~M004) |
| 관련 문서 | IA v0.6 화면구조(메뉴 A), 기능명세서 v0.8 S4-1~4-4 |

**기본 흐름**
1. 사용자가 메뉴 A 탭으로 진입한다
2. 프롬프트 입력창에 캐릭터 또는 배경에 대한 간단한 설명을 입력한다 (한국어/영어 자유)
3. 비율(1:1 기본)과 품질(medium 기본)을 선택한다
4. [생성하기] 클릭 또는 Enter
5. 시스템이 배경 이미지 1장 또는 캐릭터 다각도(정면·측면·후면) 이미지를 생성한다 — **예상 소요 시간 10~15초**, 이 구간은 스피너/스켈레톤 + "이미지를 생성하고 있습니다…" 문구로 대기 상태를 명확히 알려준다 (막연한 로딩만 보여주면 사용자가 멈춘 것으로 오인할 수 있음)
6. 결과가 결과 영역에 표시됨과 동시에 **별도 조작 없이 라이브러리에 자동 저장**된다
7. 사용자가 결과를 확인하고, 마음에 들면 [찜하기(하트)]로 즐겨찾기 표시하거나(UC-LIBRARY-03), 결과를 참고해 메뉴 B로 넘어간다

**대안/예외 흐름**
- 프롬프트가 빈값이면 [생성하기]가 비활성화 상태로 유지된다 (기본 흐름 4번 진입 불가)
- 생성 실패 시 에러 메시지와 [다시 시도]가 표시되고, 클릭 시 동일 입력값으로 재요청한다
- 생성 60초 초과 시 타임아웃 처리 후 [다시 시도] 노출 (예상 소요 시간의 4배 수준으로 여유를 둔 값)

---

## UC-IMAGE-02. 메뉴 B — 키이미지(캐릭터 시트) 생성

| 항목 | 내용 |
|------|------|
| 유즈케이스명 | 메뉴 B — 키이미지(캐릭터 시트) 생성 |
| 유즈케이스 ID | UC-IMAGE-02 |
| 주 액터 | 방송 PD/AD — 메뉴 A로 방향을 대략 잡은 뒤, 실제 촬영·제작 회의에 바로 쓸 수 있는 완성도 있는 캐릭터 자료가 필요한 실무자. 디자이너에게 의뢰할 시간 여유는 없지만, 표정·소품·컬러까지 구체적으로 정리된 레퍼런스가 있어야 다른 팀원(연출·미술)과 소통이 원활하다고 느낌 |
| 목표 | 메뉴 A에서 확인한 캐릭터 외형을 바탕으로, 실제 제작에 쓸 완성도 높은 캐릭터 시트(턴어라운드+표정+소품+컬러 팔레트)를 만든다 |
| 사전조건 | 로그인 상태일 것 / 이미지 생성 화면에서 메뉴 B 탭에 진입해 있을 것 (메뉴 A를 거치지 않고 바로 진입도 가능) |
| 사후조건 | 캐릭터 키이미지가 결과 영역에 표시되고, 생성과 동시에 라이브러리에 자동 저장됨 |
| 우선순위 | P0 (B등급 필수 — M001~M004) |
| 관련 문서 | IA v0.6 화면구조(메뉴 B), 기능명세서 v0.8 S4-1~4-4, 메뉴AB 프롬프트가이드 v1 S3 |

**기본 흐름**
1. 사용자가 메뉴 B 탭으로 진입한다
2. 입력 방식을 고른다 — **자유 텍스트**(PART1 서술+PART2 구조화 필드) 또는 **속성 선택 사이드바**(파트1 캐릭터 기본~파트5 의상·악세서리)
3-A. (자유 텍스트) PD 표준형 구조에 맞춰 프롬프트를 직접 작성한다
3-B. (속성 선택) 사이드바에서 캐릭터 속성·헤어·얼굴·의상 값을 순서대로 선택한다. 컬러 항목은 팔레트+헥스코드로 직접 지정
4. [생성하기] 클릭 또는 Enter
5. 시스템이 캐릭터 키이미지(턴어라운드+표정 배리에이션+소품 클로즈업+컬러 팔레트)를 생성한다 — 메뉴 A보다 담을 요소가 많아 **예상 소요 시간 15~20초** 수준으로 안내 (제안, 실측 후 조정)
6. 결과가 결과 영역에 표시됨과 동시에 **별도 조작 없이 라이브러리에 자동 저장**된다
7. 사용자가 결과를 확인한다 — 이후 영상 생성(UC-VIDEO-01)에서 이 캐릭터를 참고 레퍼런스로 활용, 마음에 들면 [찜하기(하트)]로 즐겨찾기 표시 가능(UC-LIBRARY-03)

**대안/예외 흐름**
- 자유 텍스트 경로에서 프롬프트가 빈값이면 [생성하기] 비활성화
- 속성 선택 사이드바 경로에서 필수 항목 미입력 시 [생성하기] 비활성화
- 생성 실패·타임아웃 시 UC-IMAGE-01과 동일한 에러 처리

---

## UC-PROMPT-01. AI 프롬프트 변환 토글 사용

| 항목 | 내용 |
|------|------|
| 유즈케이스명 | AI 프롬프트 변환 토글 사용 |
| 유즈케이스 ID | UC-PROMPT-01 |
| 주 액터 | 방송 PD/AD — 원하는 장면을 한국어로는 구체적으로 설명할 수 있지만, 영어 프롬프트 작성 경험이 거의 없어 이를 AI가 잘 이해하는 구조(구도·조명·스타일 키워드 등)로 바꾸는 데는 서투름. 별도로 프롬프트 작성법을 배울 여유는 없음 |
| 목표 | 한국어로 입력해도 AI가 이미지 생성에 최적화된 영어 프롬프트로 바꿔주도록 해, 프롬프트 엔지니어링 지식 없이도 생성 품질을 확보한다 |
| 사전조건 | 메뉴 A 또는 메뉴 B(자유 텍스트 입력 경로)에서 프롬프트 입력창에 텍스트가 입력되어 있을 것 |
| 사후조건 | 저장된 결과의 prompt 메타데이터가 변환 On 시 변환된 영문 프롬프트 기준으로 기록됨 |
| 우선순위 | P1 (필수 기능, P0 대비 우선순위만 낮음 — 디자이너·FE·BE 논의 완료, 구현 진행 중) |
| 관련 문서 | IA v0.6(전제조건 표), 기능명세서 v0.8 S4-1(공통 토글) |

**기본 흐름**
1. 사용자가 프롬프트 입력창에 한국어로 원하는 장면·캐릭터를 입력한다
2. "AI 프롬프트 변환" 토글을 On으로 켠다 (보조문구: "입력한 내용을 이미지 생성용 영어 프롬프트로 다듬어드려요")
3. [생성하기] 클릭 또는 Enter
4. 시스템이 백그라운드에서 Claude Sonnet으로 입력을 영어로 변환한다 — **변환된 영어 프롬프트는 화면에 노출하지 않는다**
5. 변환된 프롬프트로 바로 이미지 생성이 진행된다
6. 결과가 결과 영역에 표시된다

**대안/예외 흐름**
- 토글이 Off 상태면 입력한 텍스트를 그대로 생성 요청에 사용한다 (변환 단계 없음)
- 메뉴 B에서 속성 선택 사이드바 경로를 쓰는 경우 이 토글은 적용되지 않는다 (이미 구조화된 값이라 변환 대상 아님)
- 변환 자체가 실패해도 원본 입력 텍스트로 생성을 시도한다 (제안 — BE 협의 필요)

---

## UC-VIDEO-01. 영상 생성

| 항목 | 내용 |
|------|------|
| 유즈케이스명 | 영상 생성 |
| 유즈케이스 ID | UC-VIDEO-01 |
| 주 액터 | 방송 PD — 프로그램 연출·기획을 총괄하며, 실제 촬영에 들어가기 전 장면의 톤·구도·움직임을 미리 확인하고 싶어 함. AI 영상 생성 툴 사용 경험은 많지 않고, 팀 예산을 직접 관리하는 경우가 많아 생성 비용에 민감함(테스트 단계에서는 저비용 옵션 선호) |
| 목표 | B팀에서 받은 3x3 그리드 이미지와 영상 프롬프트로, **오디오가 포함된** 짧은 영상 클립을 생성해 실제 촬영 전 장면을 미리 확인한다 |
| 사전조건 | 로그인 상태일 것 / 영상 생성 화면에 진입해 있을 것 / B팀으로부터 받은 3x3 그리드 이미지를 보유하고 있을 것 |
| 사후조건 | 생성된 영상(오디오 포함)이 결과 영역에 표시되고, 완료와 동시에 라이브러리에 자동 저장됨 — **C팀 전달 조건상 오디오 포함은 필수** |
| 우선순위 | P0 (B등급 필수 — A002~A005) |
| 관련 문서 | IA v0.6 화면구조(영상 생성), 기능명세서 v0.8 S6, 프롬프트가이드 v4 S4, 마일스톤 v3.2 S7(비용 모니터링 규칙) |

> **(확인 필요 — BE 테스트 진행 중)** 아래 기본 흐름 1번(3x3 그리드 이미지 업로드)은 현재 UI 관점의 흐름이며, 실제 처리 방식은 논의 중이다. 영상 담당 BE가 Kling 3.0으로 3x3 그리드(9컷, 1번=start·9번=end 의도)를 테스트한 결과 그리드 전체가 첫 화면에 노출되거나 1번이 아닌 컷에서 시작되는 문제가 확인됨. BE에서 그리드를 9장으로 슬라이스한 뒤 Seedance 2.0 R2V(최대 9장 순서 입력 지원)에 전달하는 경로를 검토 중이나 아직 확정 아님 — 결과에 따라 아래 3번(모델 선택) 이후 흐름이 달라질 수 있다. 상세는 기능명세서 v0.8 S6-1 참고.

**기본 흐름**
1. 사용자가 영상 생성 화면에 3x3 그리드 이미지(9개 컷)를 업로드한다
2. 영상 프롬프트를 입력한다 (Shot별 장면 묘사 — 프롬프트가이드 v4 S4 기준)
3. 모델을 선택한다 — 기본값 **Kling 3.0**(개발 테스트, 저비용), 최종 데모·고품질 확인 시 **Seedance 2.0**
4. 영상 길이(5초 기본)를 선택한다
5. [영상 생성] 클릭 — **오디오는 항상 포함**되어 생성된다(C팀 전달 조건, 별도 On/Off 노출 여부는 확인 필요)
6. 시스템이 요청을 접수하고 상태를 "대기 중 → 생성 중"으로 표시한다 (5초 간격 폴링) — **예상 소요 시간은 모델·길이에 따라 수십 초~수 분** 단위로 이미지보다 훨씬 길기 때문에, "대기 중"/"생성 중" 단계 구분과 진행 표시(가짜 프로그레스 바 0→80%)로 사용자가 멈춘 게 아니라는 걸 명확히 알려준다
7. 생성 완료 시 인라인 비디오 플레이어(오디오 포함 재생)로 결과가 표시됨과 동시에 **별도 조작 없이 라이브러리에 자동 저장**된다
8. 마음에 들면 [찜하기(하트)]로 즐겨찾기 표시 가능(UC-LIBRARY-03)

**모델 선택 근거** (Kling 3.0 vs Seedance 2.0)

| 구분 | Kling 3.0 (기본값) | Seedance 2.0 |
|------|------|------|
| 비용 (오디오 미포함) | 저비용 — 개발 테스트 기본 | Kling 대비 훨씬 비쌈 |
| 비용 (오디오 포함, C팀 조건 기준) | $0.126/초 — 오디오 On 시 무음 대비 약 1.5배 상승 | $0.3024/초(720p) — 오디오 On/Off와 무관하게 동일 요금 (기능 자체는 On/Off 가능하나 과금 차이 없음) |
| 용도 | 반복 테스트, 대사·립싱크가 필요한 씬 | 최종 데모·고품질 확인용 |
| 프롬프트 구조 표준 | Scene→Characters→Action→Camera→Audio&Style | Style/Duration 헤더+Shot 라벨 (본 서비스 프롬프트 구조의 기준 모델) |
| 3x3 그리드 입력 | (확인 필요) 그리드 노출·순서 오류 확인됨 — BE 테스트 중 | R2V 경로로 9장 슬라이스 입력 검토 중 (미확정) |
| 전환 전략 | — | 안정화 확인 후 데모 직전에만 전환 (비용 통제, 마일스톤 S7 비용 모니터링 규칙) |

> 오디오를 포함한 기준으로 보면 Kling과 Seedance의 비용 격차가 무음 기준보다 좁혀진다(약 3.6배 → 약 2.4배). 다만 여전히 Kling이 저렴해 기본값 전략 자체는 유지. 3x3 그리드 이슈 테스트 결과에 따라 두 모델의 역할 분담(예: 단일 컷 자유생성 vs 스토리보드 전용)이 조정될 수 있음 — 확정 시 본 표 갱신.

**대안/예외 흐름**
- 프롬프트가 빈값이면 [영상 생성] 비활성화
- 10분 초과 시 타임아웃 에러 + [다시 시도] (새 요청으로 처음부터 재시작)
- 폴링 응답이 `ERROR` 상태면 에러 메시지 + [다시 시도]
- 화면 이탈 시 폴링은 중단되지만 서버 측 생성·자동 저장은 계속 진행됨 — 저장을 위해 화면에 머무를 필요 없음

---

## UC-VIDEO-02. 영상 재생성

| 항목 | 내용 |
|------|------|
| 유즈케이스명 | 영상 재생성 |
| 유즈케이스 ID | UC-VIDEO-02 |
| 주 액터 | 방송 PD — UC-VIDEO-01과 동일 인물. 첫 결과가 원하는 톤과 다를 때 처음부터 다시 설정을 입력하기보다, 기존 입력값을 유지한 채 필요한 부분만 빠르게 고쳐서 재시도하고 싶어 함 |
| 목표 | 생성된 영상이 마음에 들지 않을 때 프롬프트·옵션을 수정해 다시 생성한다 |
| 사전조건 | 영상 생성 결과가 화면에 표시되어 있을 것 (UC-VIDEO-01 완료 후) |
| 사후조건 | 새로운 영상 결과가 결과 영역에 표시되고 라이브러리에 별개 항목으로 자동 저장됨 (기존 결과는 삭제되지 않고 그대로 남음) |
| 우선순위 | P0 (B등급 필수 — A005) |
| 관련 문서 | 기능명세서 v0.8 S6-4 |

**기본 흐름**
1. 사용자가 결과 영역에서 [재생성] 클릭
2. 기존 프롬프트·모델·길이 값이 채워진 입력 화면으로 돌아간다
3. 사용자가 필요한 부분만 수정한다 (또는 그대로 재요청)
4. [영상 생성] 클릭 → UC-VIDEO-01의 3~7단계와 동일하게 진행
5. 새 결과가 화면에 표시되고 **라이브러리에는 기존 결과와 별개의 새 항목으로 자동 저장**된다 (화면 표시만 최신 결과로 갱신, 기존 결과는 삭제되지 않고 히스토리에 남음)

**대안/예외 흐름**
- 이전 결과에 찜 표시를 해뒀다면 재생성 후에도 그 찜 상태는 유지된다 (별개 항목이라 새 결과는 찜 표시 없이 시작)

---

## UC-LIBRARY-01. 라이브러리 조회·필터링

| 항목 | 내용 |
|------|------|
| 유즈케이스명 | 라이브러리 조회·필터링 |
| 유즈케이스 ID | UC-LIBRARY-01 |
| 주 액터 | 방송 PD/AD — 하루에도 여러 번 메뉴 A/B와 영상 생성을 반복해 결과물이 빠르게 쌓이는 상황. 예전에 만들었던 특정 캐릭터나 장면을 다시 찾아 재사용하거나 팀원에게 공유하고 싶어 함 |
| 목표 | 그동안 생성한 이미지·영상을 한곳에서 모아보고 필요한 것을 찾는다 (라이브러리 = 모든 생성 결과가 자동 저장되는 히스토리) |
| 사전조건 | 로그인 상태일 것 / 생성한 이미지·영상이 1개 이상 존재할 것 (모두 자동 저장되므로 생성만 했다면 항상 존재) |
| 사후조건 | 사용자가 원하는 생성물을 화면에서 확인한 상태 |
| 우선순위 | P0 (B등급 필수 — M005/A006) |
| 관련 문서 | 기능명세서 v0.8 S7-1 |

**기본 흐름**
1. 사용자가 라이브러리 화면으로 진입한다
2. 이미지·영상이 최신순으로 그리드에 표시된다
3. 필터 탭(전체/이미지/영상)으로 원하는 타입만 조회하거나, "찜한 것만" 필터(제안)를 켜서 즐겨찾기한 항목만 조회한다
4. 그리드 하단 [더보기]로 다음 페이지를 이어서 불러온다

**대안/예외 흐름**
- 저장된 생성물이 없으면 빈 상태 UI + [이미지 생성하러 가기] CTA가 표시된다
- 조회 실패 시 에러 상태 + [다시 시도]

---

## UC-LIBRARY-02. 라이브러리 상세보기·다운로드·삭제

| 항목 | 내용 |
|------|------|
| 유즈케이스명 | 라이브러리 상세보기·다운로드·삭제 |
| 유즈케이스 ID | UC-LIBRARY-02 |
| 주 액터 | 방송 PD/AD — 라이브러리에서 원하는 항목을 찾은 뒤, 실제 회의·편집 프로그램에서 쓸 수 있도록 원본 파일이 필요하거나, 테스트 삼아 만든 결과물이 계속 쌓여 정리가 필요하다고 느끼는 사용자 |
| 목표 | 저장된 생성물을 크게 확인하고, 필요하면 다운로드하거나 더 이상 필요 없는 항목을 정리한다 |
| 사전조건 | 라이브러리 화면에 생성물이 1개 이상 표시되어 있을 것 (UC-LIBRARY-01 이후) |
| 사후조건 | 다운로드된 파일이 로컬에 저장되거나, 삭제된 항목이 라이브러리에서 사라진 상태 |
| 우선순위 | P0 (B등급 필수 — M006/A006) |
| 관련 문서 | 기능명세서 v0.8 S7-2 |

**기본 흐름**
1. 사용자가 그리드에서 항목을 클릭한다
2. 상세 팝업이 열리고 원본 이미지/영상, 생성 당시 프롬프트, 메타 정보(모델·비율 또는 길이·생성일시), 찜 여부가 표시된다
3. [다운로드] 클릭 시 원본 파일이 브라우저로 저장된다
4. [찜하기(하트)] 클릭으로 즐겨찾기 상태를 토글할 수 있다 (상세 흐름은 UC-LIBRARY-03)
5. 더 이상 필요 없는 항목은 [삭제] 클릭 → 확인 모달("삭제하면 복구할 수 없습니다") → [삭제] 확정
6. 삭제 완료 시 팝업이 닫히고 그리드에서 즉시 제거된다

**대안/예외 흐름**
- 확인 모달에서 [취소] 선택 시 삭제가 진행되지 않고 팝업이 유지된다 (기본 포커스가 [취소])
- 다운로드·삭제 실패 시 인라인 에러 메시지와 함께 재시도 가능

---

## UC-LIBRARY-03. 찜하기(즐겨찾기) 토글

| 항목 | 내용 |
|------|------|
| 유즈케이스명 | 찜하기(즐겨찾기) 토글 |
| 유즈케이스 ID | UC-LIBRARY-03 |
| 주 액터 | 방송 PD/AD — 테스트 삼아 여러 번 생성하다 보니 라이브러리에 결과물이 빠르게 쌓여, 그중 실제로 마음에 들어 다시 쓸 것들과 그냥 시도해본 것들을 구분하고 싶어 함 |
| 목표 | 모든 생성 결과가 예외 없이 자동 저장되어 라이브러리가 계속 쌓이므로, 마음에 드는 항목만 따로 표시해 빠르게 다시 찾을 수 있게 한다 |
| 사전조건 | 생성 결과 화면 또는 라이브러리에 항목이 존재할 것 (모든 생성 결과는 자동 저장되므로 생성 직후에도 바로 사용 가능) |
| 사후조건 | 해당 항목의 찜 상태(`is_favorite`)가 토글되고, 라이브러리의 "찜한 것만" 필터에 즉시 반영됨 |
| 우선순위 | P1 (필수 기능, 자동 저장과 세트로 필요 — 디자이너·FE·BE 논의 완료) |
| 관련 문서 | 기능명세서 v0.8 S4-3, S6-3, S7-2 |

**기본 흐름**
1. 사용자가 생성 결과 화면(메뉴 A/B, 영상 생성) 또는 라이브러리 그리드/상세 팝업에서 하트 아이콘을 클릭한다
2. 시스템이 즉시 하트 아이콘 상태를 바꾸고 `PATCH /library/{id}/favorite` 호출
3. 성공 시 찜 상태가 저장되고, 라이브러리 "찜한 것만" 필터에도 바로 반영된다
4. 다시 클릭하면 찜이 해제된다 (동일 토글)

**대안/예외 흐름**
- 요청 실패 시 하트 아이콘 상태를 클릭 이전으로 되돌리고 인라인 에러 토스트 표시
- 찜은 저장 액션이 아니므로, 찜을 해제해도 라이브러리에서 항목이 사라지지 않는다 (삭제는 별도 액션, UC-LIBRARY-02)

---

## UC-REVERSE-01. 역프롬프트 — 업로드→분석→수정→재생성

| 항목 | 내용 |
|------|------|
| 유즈케이스명 | 역프롬프트 — 업로드→분석→수정→재생성 |
| 유즈케이스 ID | UC-REVERSE-01 |
| 주 액터 | 방송 PD — 레퍼런스가 될 만한 이미지(다른 프로그램 스틸컷, 무드보드 등)는 자주 접하지만, 그 스타일을 말로 옮겨 프롬프트를 처음부터 작성하는 건 부담스러워 함. "이거랑 비슷하게" 정도의 직관적인 재현을 원함 |
| 목표 | 마음에 드는 레퍼런스 이미지 하나로, 비슷한 스타일의 새 이미지를 빠르게 재현한다 |
| 사전조건 | 로그인 상태일 것 / 역프롬프트 화면에 진입해 있을 것 / 참고할 레퍼런스 이미지 파일을 보유하고 있을 것(jpg/png/webp, 10MB 이하) |
| 사후조건 | 레퍼런스 이미지 기반으로 재생성된 새 이미지가 라이브러리에 자동 저장된 상태 — 목표는 업로드부터 재생성까지 5분 이내 완료 |
| 우선순위 | P2 (S등급 도전 과제) |
| 관련 문서 | 기능명세서 v0.8 S8, 마일스톤 v3.2 S3(등급 조건) |

**기본 흐름**
1. 사용자가 레퍼런스 이미지를 업로드한다 (드래그앤드롭 또는 파일 선택)
2. [프롬프트 추출] 클릭
3. 시스템(Claude Sonnet 멀티모달)이 이미지를 분석해 스타일·조명·구도·렌즈·색감·분위기 6개 항목과 영문 프롬프트(`prompt_en`)를 추출한다
4. 추출 결과가 태그 칩 형태로, 프롬프트는 에디터에 표시된다
5. 사용자가 프롬프트를 직접 수정한다 (원본 보기 토글로 추출 원본과 비교 가능)
6. [이미지 생성으로 이어서] 클릭
7. 수정된 프롬프트가 이미지 생성 화면(메뉴 A/B)에 자동으로 채워지고, 이후 UC-IMAGE-01/UC-IMAGE-02 흐름을 그대로 따른다
8. 생성된 이미지는 별도 조작 없이 라이브러리에 자동 저장된다

**대안/예외 흐름**
- 미지원 형식·10MB 초과 업로드 시 각각 에러 메시지 표시, 업로드 진행 불가
- 분석 실패 시 에러 메시지 + [다시 시도] (업로드 파일은 유지)
- [원본으로 되돌리기]로 수정 내용을 추출 시점으로 되돌릴 수 있다

---

## 버전 히스토리

| 버전 | 날짜 | 내용 |
|------|------|------|
| v0.1 | 2026-07-11 | 초안 — 핵심 흐름 8건 작성 (메뉴 A·B 이미지 생성, AI 프롬프트 변환, 영상 생성·재생성, 라이브러리 조회·상세, 역프롬프트) |
| v0.1.1 | 2026-07-11 | AI PRD 작성 레퍼런스(Reforge) 기준 보완 — UC-01·UC-02·UC-04에 예상 소요 시간·진행 표시 안내 추가, UC-04에 Kling 3.0/Seedance 2.0 모델 선택 근거표 추가 |
| v0.2 | 2026-07-11 | 유즈케이스 메타 정보를 표 템플릿(ID·주/보조 액터·목표·사전/사후조건·우선순위·관련 문서)으로 재정리. ID 체계를 UC-01~08 순번에서 UC-{도메인}-{번호}(UC-IMAGE/PROMPT/VIDEO/LIBRARY/REVERSE)로 변경. 기본/대안 흐름 내용은 v0.1.1 유지 |
| v0.3 | 2026-07-12 | **라이브러리 저장 방식 정정** — 생성 결과는 [저장] 버튼 없이 자동 저장됨을 전 유즈케이스(UC-IMAGE-01/02, UC-VIDEO-01/02, UC-REVERSE-01)에 반영. UC-VIDEO-02는 재생성 시 기존 결과가 삭제되지 않고 별개 히스토리 항목으로 남는다는 점 명시. "찜하기" 전용 유즈케이스(UC-LIBRARY-03) 신규 추가, UC-LIBRARY-01/02에 찜 필터·토글 반영. IA v0.6·기능명세서 v0.6 기준으로 관련 문서 참조 갱신 |
| v0.4 | 2026-07-12 | **주 액터를 페르소나 수준으로 상세화** — 전 9개 유즈케이스의 "방송 PD/AD"·"방송 PD" 표기를 배경·숙련도·니즈까지 서술하는 방식으로 확장. **보조 액터 항목 전체 삭제** (템플릿·9개 유즈케이스 모두). 기능명세서 참조를 v0.7로 갱신 |
| v0.5 | 2026-07-13 | 관련 문서 참조를 기능명세서 v0.8로 전체 갱신. **UC-VIDEO-01에 오디오 포함 필수 요건 반영**(C팀 전달 조건, 목표·사후조건·기본흐름·모델비교표) / **3x3 그리드 입력 처리 방식 논의 중 각주 추가**(Kling 3.0 BE 테스트에서 그리드 노출·순서 오류 확인, Seedance 2.0 R2V 슬라이스 경로 검토 중 — 미확정) / 모델 선택 근거표에 오디오 반영 시 비용 격차 축소(약 3.6배→2.4배) 각주 추가 |
