---
title: A2팀 PRD v1
author: lsg
category: 01_기획
status: draft
created: 2026-06-25
updated: 2026-06-26
tags:
  - PRD
  - API
  - 기획
summary_target: true
---

# A2팀 PRD (Product Requirements Document) v1
> 작성일: 2026-06-25 | 업데이트: 2026-06-26 | 작성자: 이슬기, 손찬용 (PM)
> 대상: BE 2명, FE 1명 — 코딩 착수 기준 문서
> 상태: 킥오프(6/30) 전 PM 초안 → 팀 논의 후 확정
> v1.2 변경: F003 Shot 세션 관리(A등급) + 영상 이어붙이기(S등급) 추가
> v1.3 변경: 이미지 API GPT Image 2 단일화(Gemini 제외) / S등급 재정의: F003-S(ffmpeg) → F004 역프롬프트 4단계 / F004 기능 범위 확장(추출→수정→재생성→저장)

**관련 문서**
- 서비스 개요·기능 정의 → `A2팀_서비스기획서_v2_20260625.md`
- fal.ai 흐름·Claude 시스템 프롬프트 → `A2팀_BE공유_기술기획_20260625.md`
- 주차별 일정 → `A2팀_주차별마일스톤_20260625.md`

---

## 목차
1. [제품 한 줄 정의 & 범위](#1-제품-한-줄-정의--범위)
2. [유저 스토리 & 인수 조건](#2-유저-스토리--인수-조건)
3. [데이터 모델 (Supabase 스키마)](#3-데이터-모델-supabase-스키마)
4. [API 상세 명세](#4-api-상세-명세)
5. [FE 컴포넌트 요건](#5-fe-컴포넌트-요건)
6. [에러 코드 정의](#6-에러-코드-정의)
7. [환경변수 목록](#7-환경변수-목록)
8. [폴더 구조 권장안](#8-폴더-구조-권장안)
9. [MVP 외 범위 (Out of Scope)](#9-mvp-외-범위-out-of-scope)

---

## 1. 제품 한 줄 정의 & 범위

**KX GenStudio** — 방송 제작자가 텍스트 기획으로 이미지·영상을 생성하고 라이브러리에 보관하는 AI 생성 엔진

### MVP 기능 범위

| 기능 ID | 기능명 | 우선순위 | 담당 주차 |
|--------|-------|---------|---------|
| F001 | 이미지 생성 | 필수 | 2주차 |
| F003-B | 영상 생성 기본 (단일 Shot) | 필수 | 3주차 |
| F005 | 미디어 라이브러리 | 필수 | 2주차 |
| F003-A | Shot 세션 관리 + 개별 재생성 | 도전 | 3주차 후반 |
| F004 | 역프롬프트 4단계 (추출→수정→재생성→저장) | 도전 (S등급) | 4주차 |

### 등급별 달성 기준

| 등급 | 핵심 달성 조건 |
|------|-------------|
| **B** | F001 + F003-B(단일 Shot 생성·저장) + F005 + 배포 |
| **A** | F001 + F003-A(Shot 세션·재생성) + F005 + 배포 |
| **S** | A등급 + F004 역프롬프트 4단계(추출→수정→재생성→저장) |

> **F003 상한선:** F003은 A등급(Shot 세션 관리)까지. S등급 차별점은 F004 역프롬프트 완성도.

> **범위 확정 (2026-06-26):** F002 스토리보드 그리드 생성은 B팀 범위. A2팀에서 구현하지 않음.
> **구조 확정 (2026-06-26):** F003 입력 = B팀 Shot 텍스트 프롬프트(Shot1~9) + B팀 9분할 그리드 이미지. 캐릭터 일관성은 그리드 이미지가 레퍼런스 앵커 역할.

---

## 2. 유저 스토리 & 인수 조건

### F001 — 이미지 생성

#### US-01: 기본 이미지 생성
```
As 방송 AD,
I want to type a prompt and generate an image,
So that I can quickly visualize a scene without knowing AI tools.
```

**인수 조건 (Acceptance Criteria)**
- [ ] 프롬프트 입력창에 텍스트를 입력하고 [생성하기] 버튼을 클릭하면 이미지가 생성된다
- [ ] 생성 중에는 로딩 상태가 표시되며 버튼은 비활성화된다
- [ ] 생성 완료 후 결과 이미지가 화면에 표시된다
- [ ] 생성된 이미지는 자동으로 라이브러리에 저장된다
- [ ] 빈 프롬프트로 제출 시 생성 요청이 발생하지 않고 안내 문구가 표시된다

#### US-02: 모델·비율 선택
```
As 방송 PD,
I want to choose between image models and aspect ratios,
So that I can optimize for quality or speed based on the situation.
```

**인수 조건**
- [ ] 이미지 API는 GPT Image 2 단일 모델을 사용한다
- [ ] 비율 선택: 1:1 / 16:9 / 9:16 중 선택 가능
- [ ] 선택한 비율이 생성 결과에 반영된다
- [ ] 결과 이미지 카드에 생성일이 표시된다

---

### F003 — 영상 생성

#### US-03: 영상 비동기 생성
```
As 방송 PD,
I want to generate a video clip from a text prompt,
So that I can preview a scene in motion before actual shooting.
```

**인수 조건**
- [ ] 프롬프트 입력 + 모델·길이 선택 후 [영상 생성] 버튼 클릭 시 생성 요청이 제출된다
- [ ] 생성 중 상태("대기 중" → "생성 중" → "완료")가 화면에 표시된다
- [ ] 완료 후 인라인 비디오 플레이어에서 영상을 바로 재생할 수 있다
- [ ] 생성된 영상은 자동으로 라이브러리에 저장된다
- [ ] 10분 경과 후 완료되지 않으면 타임아웃 에러 메시지와 재시도 버튼이 표시된다

#### US-04: 영상 모델·길이 선택
```
As 방송 PD,
I want to choose video model and duration,
So that I can balance between cost and quality.
```

**인수 조건**
- [ ] 모델 선택: Kling 3.0 (기본, 테스트용) / Seedance 2.0 (고품질) 중 선택 가능
- [ ] 영상 길이: 5초 / 10초 / 15초 중 선택 가능
- [ ] 기본값: Kling 3.0, 5초 (비용 통제)

---

### F003-A — Shot 세션 관리 (A등급 도전 과제)

#### US-08: Shot 세션 생성 및 개별 재생성
```
As 방송 PD,
I want to manage Shot1~9 as a session and regenerate specific shots,
So that I can refine individual clips without redoing the entire storyboard.
```

**인수 조건**
- [ ] B팀 9분할 그리드 이미지 + Shot1~9 텍스트 프롬프트를 업로드하면 세션이 생성된다
- [ ] 세션 화면에 Shot1~9 카드가 순서대로 나열되고 각각 생성 상태가 표시된다
- [ ] [전체 생성] 버튼으로 Shot1~9를 순차적으로 일괄 생성 요청할 수 있다
- [ ] 각 Shot 카드에 [재생성] 버튼이 있어 해당 Shot만 단독으로 재생성할 수 있다
- [ ] 재생성 중에도 다른 Shot의 영상은 영향받지 않는다
- [ ] 완료된 Shot 카드에서 영상을 인라인으로 미리볼 수 있다

---


### F005 — 미디어 라이브러리

#### US-05: 생성물 보관 및 조회
```
As 방송 AD,
I want to see all generated images and videos in one place,
So that I can manage and reuse my creative assets.
```

**인수 조건**
- [ ] 라이브러리 탭에 이미지·영상이 생성 최신순으로 그리드 형태로 표시된다
- [ ] 이미지/영상 타입 필터로 원하는 타입만 볼 수 있다
- [ ] 아이템을 클릭하면 상세 모달이 열린다
- [ ] 모달에서 다운로드 버튼으로 파일을 저장할 수 있다
- [ ] 아이템이 없을 때 빈 상태 UI와 생성 시작 안내가 표시된다
- [ ] 20개 초과 시 추가 로드(페이지네이션 또는 무한스크롤)가 동작한다

---

### F004 — 역프롬프트 (S등급 도전 과제)

#### US-07: 레퍼런스 이미지 → 프롬프트 추출 → 수정 → 재생성 → 저장

```
As 방송 PD,
I want to upload a reference image, extract a prompt, refine it, and regenerate a new image on the same page,
So that I can recreate a similar style without switching tools.
```

**인수 조건 — 1단계: 추출**
- [ ] 이미지 파일(JPG/PNG, 최대 10MB)을 업로드할 수 있다
- [ ] [프롬프트 추출] 클릭 시 Claude Sonnet이 분석해 스타일·조명·구도·렌즈·색감·분위기 항목이 담긴 영문 프롬프트를 반환한다
- [ ] 추출된 프롬프트는 복사 버튼과 함께 표시된다

**인수 조건 — 2단계: 수정**
- [ ] 추출된 프롬프트를 텍스트 에디터에서 직접 수정할 수 있다
- [ ] 수정 중에도 원본 추출 프롬프트를 확인할 수 있다 (원본 보기 토글)

**인수 조건 — 3단계: 재생성**
- [ ] [이 프롬프트로 이미지 생성] 버튼 클릭 시 동일 페이지에서 GPT Image 2로 이미지를 생성한다
- [ ] 생성 중 로딩 상태가 표시된다
- [ ] 생성 완료 후 결과 이미지가 동일 페이지에 표시된다

**인수 조건 — 4단계: 저장**
- [ ] 생성된 이미지가 자동으로 라이브러리에 저장된다
- [ ] 저장 완료 후 라이브러리 이동 링크가 표시된다

---

## 3. 데이터 모델 (Supabase 스키마)

### 3.1 테이블: `media_items`

이미지와 영상을 단일 테이블로 관리합니다.

```sql
CREATE TABLE media_items (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  type        TEXT NOT NULL CHECK (type IN ('image', 'video')),
  status      TEXT NOT NULL DEFAULT 'completed'
              CHECK (status IN ('pending', 'processing', 'completed', 'failed')),
  
  -- 생성 정보
  prompt      TEXT,                        -- 사용자 입력 프롬프트
  model       TEXT,                        -- 사용한 API 모델명
  aspect_ratio TEXT,                       -- 이미지 비율 (1:1, 16:9, 9:16)
  duration    INTEGER,                     -- 영상 길이 (초, 영상만)
  
  -- 파일 정보
  storage_path TEXT,                       -- Supabase Storage 경로
  public_url  TEXT,                        -- 공개 접근 URL
  thumbnail_url TEXT,                      -- 썸네일 URL (영상만)
  file_size   INTEGER,                     -- 파일 크기 (bytes)
  
  -- 비동기 처리 (영상만)
  fal_request_id TEXT,                     -- fal.ai queue request_id
  
  -- 메타데이터
  created_at  TIMESTAMPTZ DEFAULT NOW(),
  updated_at  TIMESTAMPTZ DEFAULT NOW()
);

-- 최신순 조회용 인덱스
CREATE INDEX idx_media_items_created_at ON media_items(created_at DESC);
CREATE INDEX idx_media_items_type ON media_items(type);
```

### 3.2 테이블: `storyboard_sessions` (A등급~)

Shot 세션 관리용. Shot1~9를 하나의 작업 단위로 묶습니다.

```sql
CREATE TABLE storyboard_sessions (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  grid_image_url  TEXT NOT NULL,   -- B팀 9분할 그리드 이미지 URL
  status          TEXT NOT NULL DEFAULT 'in_progress'
                  CHECK (status IN ('in_progress', 'completed')),
  created_at      TIMESTAMPTZ DEFAULT NOW(),
  updated_at      TIMESTAMPTZ DEFAULT NOW()
);
```

### 3.3 테이블: `storyboard_shots` (A등급~)

세션 내 개별 Shot 상태 및 결과를 관리합니다.

```sql
CREATE TABLE storyboard_shots (
  id             UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  session_id     UUID NOT NULL REFERENCES storyboard_sessions(id) ON DELETE CASCADE,
  shot_number    INTEGER NOT NULL CHECK (shot_number BETWEEN 1 AND 9),
  prompt         TEXT NOT NULL,    -- B팀 제공 Shot 텍스트 프롬프트
  status         TEXT NOT NULL DEFAULT 'pending'
                 CHECK (status IN ('pending', 'processing', 'completed', 'failed')),
  video_url      TEXT,
  fal_request_id TEXT,
  created_at     TIMESTAMPTZ DEFAULT NOW(),
  updated_at     TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE (session_id, shot_number)
);

CREATE INDEX idx_storyboard_shots_session ON storyboard_shots(session_id, shot_number);
```

### 3.4 Supabase Storage 버킷 구조

```
버킷: kx-genstudio
  ├── images/
  │   └── {uuid}.png
  └── videos/
      └── {uuid}.mp4
```

### 3.3 데이터 흐름

**이미지 생성 시:**
```
1. POST /api/generate-image 호출
2. API에서 이미지 생성 성공
3. Supabase Storage에 이미지 업로드 → storage_path, public_url 획득
4. media_items 테이블에 INSERT (status: 'completed')
5. FE에 { id, public_url } 반환
```

**영상 생성 시:**
```
1. POST /api/generate-video 호출
2. fal.ai queue.submit() → request_id 획득
3. media_items에 INSERT (status: 'processing', fal_request_id: request_id)
4. FE에 { id, requestId, status: 'processing' } 반환
5. FE가 5초 간격으로 GET /api/video-status?id={id} 폴링
6. 완료 시: fal.ai에서 영상 URL 획득 → Storage에 저장 → media_items UPDATE (status: 'completed')
7. FE에 { status: 'completed', public_url } 반환
```

---

## 4. API 상세 명세

> fal.ai 비동기 흐름 상세 및 Claude 시스템 프롬프트는 `A2팀_BE공유_기술기획_20260625.md` 참조

### 4.1 `POST /api/generate-image`

**Request**
```typescript
{
  prompt: string        // 필수, 1자 이상
  model: 'gpt-image-2'                    // 단일 모델 고정
  aspectRatio: '1:1' | '16:9' | '9:16'   // 기본값: '1:1'
}
```

**Response — 성공 (200)**
```typescript
{
  success: true,
  data: {
    id: string           // media_items.id
    public_url: string   // 이미지 URL
    model: string
    aspect_ratio: string
    created_at: string
  }
}
```

**Response — 실패 (400 / 500)**
```typescript
{
  success: false,
  error: {
    code: 'INVALID_INPUT' | 'GENERATION_FAILED' | 'API_LIMIT_EXCEEDED',
    message: string      // 사용자에게 표시할 메시지 (한국어)
  }
}
```

**GPT Image 2 API 호출 참고**
```typescript
import OpenAI from 'openai';
const openai = new OpenAI();

const response = await openai.images.generate({
  model: 'gpt-image-1',
  prompt: prompt,
  size: aspectRatio === '16:9' ? '1792x1024'
       : aspectRatio === '9:16' ? '1024x1792'
       : '1024x1024',
  quality: 'medium',    // 테스트: medium / 데모: high
  output_format: 'png',
});
// response.data[0].b64_json → base64 이미지
```

---

### 4.2 `POST /api/generate-video`

**Request**
```typescript
{
  prompt: string                              // 필수
  model: 'kling-3' | 'seedance-2'            // 기본값: 'kling-3'
  duration: 5 | 10 | 15                      // 기본값: 5
  referenceImageUrl?: string                 // B팀 9분할 그리드 이미지 URL (선택)
  shotId?: string                            // storyboard_shots.id (A등급: 세션 내 Shot)
}
```

**Response — 성공 (200)**
```typescript
{
  success: true,
  data: {
    id: string           // media_items.id (DB에 저장된 레코드 ID)
    requestId: string    // fal.ai request_id (폴링에 사용)
    status: 'processing'
  }
}
```

---

### 4.3 `GET /api/video-status`

**Query Params**
```
?id={media_items.id}
```

**Response — 처리 중 (200)**
```typescript
{
  success: true,
  data: { id: string, status: 'processing' }
}
```

**Response — 완료 (200)**
```typescript
{
  success: true,
  data: {
    id: string,
    status: 'completed',
    public_url: string,     // 영상 URL
    thumbnail_url: string   // 썸네일 URL (있을 경우)
  }
}
```

**Response — 실패 (200)**
```typescript
{
  success: true,
  data: {
    id: string,
    status: 'failed',
    error: { code: string, message: string }
  }
}
```

> **폴링 로직 (FE 구현 기준):** 5초 간격, 최대 120회 (10분). 120회 초과 시 클라이언트 측 타임아웃으로 처리.

---

### 4.4 `GET /api/library`

**Query Params**
```
?type=image|video               // 생략 시 전체
&page=1                         // 기본값: 1
&limit=20                       // 기본값: 20
```

**Response — 성공 (200)**
```typescript
{
  success: true,
  data: {
    items: Array<{
      id: string
      type: 'image' | 'video'
      status: string
      public_url: string
      thumbnail_url: string | null
      prompt: string
      model: string
      duration: number | null
      created_at: string
    }>,
    total: number,
    page: number,
    has_more: boolean
  }
}
```

---

### 4.5 `POST /api/storyboard-session` (A등급 도전 과제)

세션 생성 + Shot1~9 일괄 생성 시작.

**Request**
```typescript
{
  gridImageUrl: string          // B팀 9분할 그리드 이미지 URL
  shots: Array<{
    shotNumber: number          // 1~9
    prompt: string              // B팀 Shot 텍스트 프롬프트
  }>
  model: 'kling-3' | 'seedance-2'
  duration: 5 | 10 | 15
}
```

**Response — 성공 (200)**
```typescript
{
  success: true,
  data: {
    sessionId: string
    shots: Array<{
      id: string
      shotNumber: number
      status: 'processing'
    }>
  }
}
```

> BE 처리: storyboard_sessions INSERT → storyboard_shots 9개 INSERT → 각 Shot별 fal.ai queue.submit() → request_id를 storyboard_shots에 저장. FE는 기존 `/api/video-status` 폴링을 Shot별로 실행.

---

### 4.6 `POST /api/storyboard-shot/:shotId/regenerate` (A등급 도전 과제)

특정 Shot만 재생성. 세션의 다른 Shot은 변경하지 않습니다.

**Request**
```typescript
{
  model?: 'kling-3' | 'seedance-2'   // 생략 시 기존 세션 모델
  duration?: 5 | 10 | 15
}
```

**Response — 성공 (200)**
```typescript
{
  success: true,
  data: {
    id: string          // storyboard_shots.id
    status: 'processing'
    fal_request_id: string
  }
}
```

---

### 4.7 `GET /api/storyboard-session/:sessionId` (A등급 도전 과제)

세션 전체 상태 조회 (FE 진입 시 or 새로고침 후 상태 복원용).

**Response — 성공 (200)**
```typescript
{
  success: true,
  data: {
    id: string
    gridImageUrl: string
    status: 'in_progress' | 'completed'
    shots: Array<{
      id: string
      shotNumber: number
      prompt: string
      status: 'pending' | 'processing' | 'completed' | 'failed'
      videoUrl: string | null
    }>
  }
}
```

---

### 4.8 `POST /api/reverse-prompt-generate` (S등급 도전 과제)

역프롬프트 추출 후 수정된 프롬프트로 이미지 재생성.

**Request**
```typescript
{
  prompt: string    // 수정된 프롬프트 (추출 결과를 사용자가 편집한 값)
  aspectRatio: '1:1' | '16:9' | '9:16'   // 기본값: '1:1'
}
```

**Response — 성공 (200)**
```typescript
{
  success: true,
  data: {
    id: string           // media_items.id
    public_url: string   // 재생성된 이미지 URL
    created_at: string
  }
}
```

> `/api/generate-image`와 동일 로직 재사용 가능. media_items에 자동 저장.

---

### 4.9 `POST /api/reverse-prompt` (S등급 도전 과제 — 1단계)

**Request (multipart/form-data)**
```
image: File   // JPG or PNG, 최대 10MB
```

**Response — 성공 (200)**
```typescript
{
  success: true,
  data: {
    analysis: {
      style: string
      lighting: string
      composition: string
      lens: string
      color_palette: string
      mood: string
    },
    prompt_en: string   // 재현용 통합 프롬프트
  }
}
```

---

## 5. FE 컴포넌트 요건

### 5.1 페이지 구조

```
app/
├── page.tsx                           ← 홈
├── generate/image/page.tsx            ← F001 이미지 생성
├── generate/video/page.tsx            ← F003-B 영상 생성 (단일 Shot)
├── storyboard/page.tsx                ← F003-A Shot 세션 목록
├── storyboard/[sessionId]/page.tsx    ← F003-A Shot 에디터
├── reverse-prompt/page.tsx            ← F004 역프롬프트
└── library/page.tsx                   ← F005 미디어 라이브러리
```

### 5.2 공통 컴포넌트

| 컴포넌트 | 위치 | 요건 |
|---------|------|------|
| `Header` | `components/layout/Header.tsx` | 로고 + 탭 네비게이션 (현재 탭 활성 표시) |
| `PromptInput` | `components/common/PromptInput.tsx` | textarea, 최소 1자 검증, 글자 수 표시 (선택) |
| `ModelSelect` | `components/common/ModelSelect.tsx` | 드롭다운, 모델명·설명 표시 |
| `LoadingSpinner` | `components/common/LoadingSpinner.tsx` | 전체화면 오버레이 or 인라인 |
| `ErrorMessage` | `components/common/ErrorMessage.tsx` | 에러 코드별 한국어 메시지 + 재시도 버튼 |
| `EmptyState` | `components/common/EmptyState.tsx` | 빈 상태 일러스트 + 안내 텍스트 + CTA 버튼 |

### 5.3 기능별 핵심 컴포넌트

**이미지 생성 (F001)**

| 컴포넌트 | 요건 |
|---------|------|
| `ImageGenerateForm` | PromptInput + ModelSelect + AspectRatioSelect + 생성 버튼. 생성 중 버튼 비활성화. |
| `ImageResultGrid` | 생성된 이미지 카드 그리드. 최근 결과가 맨 앞. 카드 호버 시 저장/다운로드 버튼 표시. |
| `ImageCard` | 썸네일 + 모델명 + 생성일. 클릭 시 모달 열림. |

**영상 생성 기본 — F003-B**

| 컴포넌트 | 요건 |
|---------|------|
| `VideoGenerateForm` | PromptInput + ModelSelect + DurationSelect + referenceImageUrl 입력(선택) + 생성 버튼 |
| `VideoStatusDisplay` | `pending` → `processing` → `completed` 상태 표시. `processing` 중 가짜 프로그레스 바(0→80%). 완료 시 VideoPlayer 전환. |
| `VideoPlayer` | HTML5 `<video>` + controls. 자동재생(muted). |

**Shot 세션 에디터 — F003-A (A등급)**

| 컴포넌트 | 요건 |
|---------|------|
| `StoryboardEditor` | 페이지 전체 레이아웃. 좌측 그리드 이미지 미리보기 + 우측 Shot 카드 목록 + 하단 이어붙이기 영역. |
| `ShotCard` | Shot 번호, 프롬프트 요약, 상태(pending/processing/completed/failed), 인라인 VideoPlayer, [재생성] 버튼. 상태별 UI 분기. |
| `SessionProgressBar` | 전체 9개 중 완료된 Shot 수 표시. `4 / 9 완료` 형태. |

**역프롬프트 — F004 (S등급)**

| 컴포넌트 | 요건 |
|---------|------|
| `ReversePromptUpload` | 이미지 드래그앤드롭 or 파일 선택. JPG/PNG, 최대 10MB 검증. |
| `ExtractedPromptEditor` | 추출된 프롬프트 텍스트 에디터 + 원본 보기 토글 + 복사 버튼. |
| `RegenerateButton` | 수정된 프롬프트로 GPT Image 2 이미지 재생성 요청. 생성 중 비활성화. |
| `ReverseResultDisplay` | 재생성 이미지 표시 + 라이브러리 이동 링크. |

**라이브러리 (F005)**

| 컴포넌트 | 요건 |
|---------|------|
| `LibraryGrid` | 이미지·영상 혼합 그리드. 타입 필터 탭. 페이지네이션 or 무한스크롤. |
| `MediaModal` | 이미지: 원본 크기 표시 + 다운로드. 영상: VideoPlayer + 다운로드. 공통: 프롬프트·모델·생성일 표시. |

### 5.4 상태 관리 방침

- **전역 상태 불필요** — 각 페이지가 독립적으로 동작
- **영상 폴링 상태**는 페이지 레벨 `useState` + `useEffect`로 관리
- **라이브러리**는 페이지 진입 시 API 호출로 최신 데이터 패치

```typescript
// 영상 폴링 예시 (FE 참고)
const [videoStatus, setVideoStatus] = useState<'idle' | 'processing' | 'completed' | 'failed'>('idle');
const [mediaId, setMediaId] = useState<string | null>(null);

useEffect(() => {
  if (!mediaId || videoStatus !== 'processing') return;

  const interval = setInterval(async () => {
    const res = await fetch(`/api/video-status?id=${mediaId}`);
    const { data } = await res.json();
    
    if (data.status === 'completed' || data.status === 'failed') {
      setVideoStatus(data.status);
      clearInterval(interval);
    }
  }, 5000);

  // 10분 타임아웃
  const timeout = setTimeout(() => {
    setVideoStatus('failed');
    clearInterval(interval);
  }, 600000);

  return () => { clearInterval(interval); clearTimeout(timeout); };
}, [mediaId, videoStatus]);
```

### 5.5 로딩·에러·빈 상태 처리 원칙

모든 비동기 동작에 대해 아래 세 가지 상태를 반드시 구현합니다.

| 상태 | 표시 내용 |
|------|---------|
| **Loading** | 스피너 또는 스켈레톤 UI. 버튼 비활성화. |
| **Error** | 에러 메시지(한국어) + 재시도 버튼. 콘솔에 에러 코드 로그. |
| **Empty** | 빈 상태 안내 문구 + 해당 기능 시작 CTA 버튼. |

---

## 6. 에러 코드 정의

BE·FE가 동일하게 사용하는 에러 코드 목록입니다.

| 코드 | HTTP Status | 원인 | FE 표시 메시지 (한국어) |
|------|------------|------|----------------------|
| `INVALID_INPUT` | 400 | 빈 프롬프트, 잘못된 파라미터 | "입력값을 확인해 주세요." |
| `FILE_TOO_LARGE` | 400 | 업로드 파일 10MB 초과 | "파일 크기가 너무 큽니다. 10MB 이하 파일을 업로드해 주세요." |
| `UNSUPPORTED_FORMAT` | 400 | 지원하지 않는 파일 형식 | "JPG 또는 PNG 파일만 업로드 가능합니다." |
| `GENERATION_FAILED` | 500 | API 생성 실패 | "이미지 생성에 실패했습니다. 다시 시도해 주세요." |
| `VIDEO_GENERATION_FAILED` | 500 | 영상 API 실패 | "영상 생성에 실패했습니다. 다시 시도해 주세요." |
| `TIMEOUT` | 504 | 영상 생성 10분 초과 | "영상 생성 시간이 초과되었습니다. 다시 시도해 주세요." |
| `API_LIMIT_EXCEEDED` | 429 | API 비용 한도 초과 | "현재 사용량 한도에 도달했습니다. 잠시 후 다시 시도해 주세요." |
| `STORAGE_FAILED` | 500 | Supabase 저장 실패 | "저장 중 오류가 발생했습니다. 다시 시도해 주세요." |
| `UNKNOWN` | 500 | 예상치 못한 에러 | "알 수 없는 오류가 발생했습니다. 다시 시도해 주세요." |

---

## 7. 환경변수 목록

> `.env.local` 파일에 설정. 팀 공유 시 `.env.example`로 키 이름만 공유 (값 제외).

```bash
# OpenAI — 이미지 생성 (F001)
OPENAI_API_KEY=

# Anthropic — 역프롬프트 (F004)
ANTHROPIC_API_KEY=

# fal.ai — 영상 생성 (F003)
FAL_KEY=

# Supabase — 라이브러리 저장 (F005)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=     # 서버사이드 전용 (API Route에서만 사용)

# 앱 설정
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

> **보안 주의:** `SUPABASE_SERVICE_ROLE_KEY`는 절대 클라이언트(브라우저)에 노출되면 안 됩니다. `NEXT_PUBLIC_` 접두사 없이 사용.

---

## 8. 폴더 구조 권장안

```
kx-genstudio/
├── app/
│   ├── layout.tsx
│   ├── page.tsx                       ← 홈
│   ├── generate/
│   │   ├── image/page.tsx             ← F001
│   │   ├── storyboard/page.tsx        ← F002
│   │   └── video/page.tsx             ← F003
│   ├── reverse-prompt/page.tsx        ← F004
│   ├── library/page.tsx               ← F005
│   └── api/
│       ├── generate-image/route.ts
│       ├── generate-video/route.ts
│       ├── video-status/route.ts
│       ├── storyboard-session/route.ts          ← A등급
│       ├── storyboard-shot/[shotId]/
│       │   └── regenerate/route.ts              ← A등급
│       ├── reverse-prompt/route.ts               ← F004 (S등급)
│       └── library/route.ts
│
├── components/
│   ├── layout/
│   │   └── Header.tsx
│   └── common/
│       ├── PromptInput.tsx
│       ├── ModelSelect.tsx
│       ├── LoadingSpinner.tsx
│       ├── ErrorMessage.tsx
│       └── EmptyState.tsx
│
├── lib/
│   ├── openai.ts              ← OpenAI 클라이언트
│   ├── anthropic.ts           ← Anthropic 클라이언트
│   ├── fal.ts                 ← fal.ai 클라이언트
│   ├── supabase/
│   │   ├── client.ts          ← 브라우저용 클라이언트
│   │   └── server.ts          ← 서버용 클라이언트 (API Route)
│   └── errors.ts              ← 에러 코드 상수 정의
│
├── types/
│   └── index.ts               ← 공통 타입 정의
│
└── .env.local
```

---

## 9. MVP 외 범위 (Out of Scope)

이번 MVP에서 **구현하지 않는** 것들입니다. 요청이 와도 이번 인턴십 내에서는 거절합니다.

| 항목 | 이유 |
|------|------|
| 회원가입·로그인·인증 | 팀 내 데모용으로 인증 불필요. Supabase Auth 연동은 고도화 단계 |
| 영상 클립 이어붙이기 (ffmpeg) | 이번 MVP 범위 외. S등급은 역프롬프트 4단계로 대체 |
| 실시간 이미지 렌더링 (Krea 방식) | 레이턴시·비용 모두 MVP 수준 초과 |
| 캐릭터 일관성 보장 (Soul ID) | 현재 이미지 생성 모델의 기술적 한계 |
| 팀 협업·공유 기능 | 고도화 단계 |
| 소셜 로그인 | 고도화 단계 |
| 모바일 앱 | 웹 반응형으로 대체 |
| 다국어 UI | 한국어 단일 UI |

---

*본 문서는 킥오프(6/30) 후 팀 논의를 반영해 업데이트됩니다.*
*변경 사항은 상단 버전 이력에 기록합니다.*
