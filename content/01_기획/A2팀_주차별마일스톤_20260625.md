# A2팀 주차별 마일스톤
> 작성일: 2026-06-25 | 업데이트: 2026-06-26 | 기준: 프로젝트 기간 2026.06.30 ~ 07.29 (4주 / 하루 3시간)
> 대상 기능: F001 이미지 생성 · F003 영상 생성(3단계) · F004 역프롬프트 · F005 미디어 라이브러리
> **변경:** F002 B팀 확정 제외 / F003을 B·A·S 3단계로 세분화 (Shot 세션 관리·이어붙이기 추가)

---

## 목표 등급 기준

| 등급 | 핵심 조건 |
|------|---------|
| **S (도전)** | F001 + F003-A + F004 역프롬프트 4단계 + F005 + 배포 |
| **A (목표)** | F001 + F003-A(Shot 세션) + F005 + 배포 |
| **B (최소 완주선)** | F001 + F003-B(단일 Shot) + F005 + 배포 |

### F003 단계별 정의

| 단계 | 기능 | 핵심 요건 |
|------|------|---------|
| **F003-B** (B등급) | 단일 Shot 영상 생성 | B팀 Shot 프롬프트 + 9분할 그리드 이미지 → 영상 1개 생성·저장 |
| **F003-A** (A등급~) | Shot 세션 관리 | Shot1~9 개별 생성, 상태 추적 UI, 특정 Shot 재생성 가능 |

### F004 단계별 정의 (S등급)

| 단계 | 내용 |
|------|------|
| 1단계 추출 | 레퍼런스 이미지 업로드 → Claude Sonnet 분석 → 영문 프롬프트 반환 |
| 2단계 수정 | 추출된 프롬프트를 텍스트 에디터에서 직접 편집 |
| 3단계 재생성 | 수정된 프롬프트로 GPT Image 2 이미지 재생성 (동일 페이지) |
| 4단계 저장 | 재생성 이미지 자동 라이브러리 저장 |

> **판단 포인트:** 3주차 목요일에 F003-B 안정성 기준으로 F003-A 착수 여부 결정. 4주차 초에 F003-A 완료 기준으로 F004 착수 여부 결정.

---

## 팀 구성 및 역할 요약

| 직무 | 인원 | 주요 담당 |
|------|------|---------|
| PM | 손찬용, 이슬기 | 기획·일정·QA·발표 |
| BE | 2명 | API Route, Supabase, 비동기 처리 |
| FE | 1명 | UI 컴포넌트, API 연결, 배포 |
| UX/UI | 1명 | 와이어프레임, 디자인 시스템, 화면 디자인 |

---

## 1주차 — 환경 세팅 & 구조 완성

**주간 목표:** 개발 환경 완비 + FE·BE 구조 완성 (API 실제 호출은 키 도착 즉시 진행, 1주차 내 완료 필수 아님)

> **일정 특이사항:** 1일차(월)는 PM 2인 선행 작업 / 2일차(화) 오프라인 팀 미팅 — 미팅 당일은 실무 거의 없음 / 실질적 개발 착수는 3일차(수)부터
> **API 키 정책:** 킥오프 전 PD님께 발급 요청 선행 (결재→경영지원팀→결제 최대 3일 소요 가능) → 키 미도착 시에도 mock 응답으로 구조 작업 병행, 키 도착 즉시 실제 호출 전환

### 미팅 전 PM 사전 준비 (1일차 월)

| 준비 항목 | 내용 |
|---------|------|
| 서비스 기획서 v2 최종 정리 | 팀 공유 전 마지막 확인 (기능 범위·API 전략·A-B 연동 흐름) |
| 킥오프 미팅 아젠다 확정 | 역할 분담표, 기술 스택, 브랜치 전략, 비용 정책, Supabase 스키마 방향 |
| 의사결정 항목 목록화 | 서비스명·모델 확정·IA 확정 등 미팅에서 결정할 사항 정리 |
| fal.ai Hard Limit $120 요청 준비 | 매니저 전달용 요청 내용 초안 (미팅 전 메모 완비) |
| API 키 발급 요청 목록 작성 | OpenAI·fal.ai·Anthropic·Supabase 키 발급 절차 정리 → 미팅 때 BE에 전달 |

### 주차별 일정

| 일차 | PM | BE | FE | UX/UI |
|------|----|----|----|----|
| **1일 (월)** ⚠️ PM 단독 | 서비스 기획서 v2 최종 정리, 킥오프 아젠다 작성, 미팅 준비 자료 완비 | — | — | — |
| **2일 (화)** ⚠️ 팀 미팅 | **오프라인 팀 킥오프 미팅** — 기획 공유·역할 확정·기술 스택 합의·브랜치 전략·Supabase 스키마 방향·비용 정책·일정 공유 / 미팅 후 결정 사항 문서화 | 레포 생성·브랜치 전략 합의 (미팅 중), API 키 발급 신청 | 개발 환경 확인 (미팅 후) | 레퍼런스 수집 시작 (미팅 후) |
| **3일 (수)** | 화면 IA 확정 문서화 (4개 탭), fal.ai 비동기 흐름 (submit → polling → result) 문서화 | `/api/generate-image` Route 구조 작성 (mock 응답, GPT Image 2 단일 모델), Supabase 프로젝트 생성·스키마 초안 (free tier 활용) | Next.js 14 App Router 보일러플레이트 세팅, Tailwind + TypeScript 구성, 라우팅 구조 세팅 (4개 탭) | 레퍼런스 수집 (Krea·Higgsfield·Magnific), 무드보드·와이어프레임 1차 (이미지 생성·영상 생성 탭) |
| **4일 (목)** | 이미지·영상 프롬프트 가이드 초안, Claude Sonnet 역프롬프트 출력 형식 설계 | `/api/generate-video` · `/api/video-status` Route 구조 작성 (mock), Supabase 스키마 확정·버킷 세팅 / **키 도착 시** GPT Image 2 + fal.ai 실제 호출 전환 | 공통 컴포넌트 제작 (버튼·입력창·드롭다운·로딩 스피너), mock API 연결로 UI 흐름 확인 | 와이어프레임 2차 (역프롬프트·라이브러리 탭), 디자인 시스템 컬러·타이포·컴포넌트 정의 |
| **5일 (금)** | 1주차 점검 (구조 완성 여부·키 도착 여부 확인), fal.ai Hard Limit $120 설정 요청 매니저 전달, 2주차 플랜 공유 | 환경변수 정리 (.env.local), **키 도착 완료 시** API 실제 호출 검증·응답 구조 파악 / 미도착 시 mock 구조 고도화 | 환경변수 연결 확인, 기본 레이아웃 QA, mock 흐름 시연 가능 상태 | 디자인 시스템 버튼·카드·상태(로딩·에러·빈 상태) 컴포넌트 디자인, 주요 화면 디자인 착수 |

**1주차 완료 기준**
- [ ] 레포 세팅 완료, 팀 전원 로컬 실행 가능
- [ ] FE 라우팅·공통 컴포넌트 구조 완성, mock 흐름 동작 확인
- [ ] BE API Route 구조 완성 (mock 응답 포함)
- [ ] Supabase 스키마 확정·버킷 세팅 완료 (free tier)
- [ ] 화면 IA 확정
- [ ] (키 도착 시) GPT Image 2 + fal.ai 실제 호출 1회 성공 — **미달성 시 2주차 월요일로 순연**

---

## 2주차 — 이미지 생성 + 라이브러리 완성 ⭐ 중간점검

**주간 목표:** 프롬프트 → 이미지 생성 → 라이브러리 저장 전체 플로우 동작 (중간점검 Pass)

| 일차 | PM | BE | FE | UX/UI |
|------|----|----|----|----|
| **1일 (월)** | 테스트 프롬프트 세트 20개 작성 (방송 제작 시나리오 기반), 이미지 품질 판단 기준 정의 | F001 이미지 생성 API Route 완성 `/api/generate-image` (GPT Image 2·Gemini 모델 분기, 비율 옵션 처리) | 이미지 생성 탭 UI 완성 (프롬프트 입력창·모델 선택·비율 옵션·생성 버튼) | 이미지 생성 탭 최종 디자인, 입력 인터랙션 (비활성화·로딩·에러 상태) |
| **2일 (화)** | 사용성 테스트 시나리오 작성 (비개발자 기준, 5분 내 이미지 1장 생성), 발표 데모 스토리 초안 | 에러 핸들링·응답 표준화, 생성 실패 시 재시도 처리 | 생성 결과 그리드 표시 완성 (이미지 카드·호버 인터랙션) | 결과 카드 디자인 (썸네일·모델명·생성일 표시, 호버 시 액션 버튼) |
| **3일 (수)** | 외부 사용성 테스트 진행 (비개발자 1명, 프롬프트 → 이미지 플로우), 관찰 기록 | F005 라이브러리 저장 API `/api/library` (Supabase Storage 업로드 + DB 저장), 조회 API 구현 | 라이브러리 저장·조회 FE 연결, 생성 후 자동 저장 플로우 | 라이브러리 탭 디자인 (이미지·영상 통합 그리드, 필터 탭) |
| **4일 (목)** | 사용성 테스트 피드백 정리 → 백로그 반영, 프롬프트 가이드 v2 업데이트 | 라이브러리 페이지네이션·타입 필터(이미지/영상) API, 이미지 다운로드 처리 | 라이브러리 탭 UI 완성 (그리드·상세 모달·다운로드 버튼), 빈 상태 화면 | 라이브러리 상세 모달 디자인, 빈 상태·에러 UI, 마이크로카피 |
| **5일 (금)** | **중간점검 발표** (Pass 기준 점검·회고), 3주차 플랜 공유 | 통합 테스트 (프롬프트 → 이미지 → 저장 → 라이브러리 조회), 버그픽스 | 로딩·에러 상태 UI 전체 적용, 통합 QA | 중간점검 데모용 화면 정리, 발표 자료 화면 캡처 |

**2주차 완료 기준 (중간점검 Pass)**
- [ ] 프롬프트 → GPT Image 2 이미지 생성 동작
- [ ] 생성 이미지 Supabase 저장 + 라이브러리 조회 동작
- [ ] 이미지 다운로드 동작
- [ ] 로딩·에러 상태 UI 처리 완료

---

## 3주차 — F003-B 영상 생성 완성 → F003-A Shot 세션 착수

**주간 목표:** F003-B (단일 Shot 생성) 안정화 → 목요일 판단 후 F003-A (Shot 세션) 착수

| 일차 | PM | BE | FE | UX/UI |
|------|----|----|----|----|
| **1일 (월)** | 영상 프롬프트 가이드 작성 (씬 묘사·카메라 무브먼트 키워드), B팀 그리드 이미지 레퍼런스 활용 방식 BE 공유 | F003-B: `/api/generate-video` (fal.ai queue submit + referenceImageUrl 파라미터 포함), Kling 5초 테스트 우선 | 영상 생성 탭 UI (프롬프트 입력·모델 선택·영상 길이·그리드 이미지 업로드 선택) | 영상 생성 탭 디자인, 비동기 진행 상태 화면 (생성 중 / 완료 / 실패) |
| **2일 (화)** | 비동기 UX 점검 (진행 상태 표시 방식), fal.ai 대시보드 비용 확인 | F003-B: `/api/video-status` (polling용), 완료 시 영상 URL 반환 | 진행률 UI + polling 연결 (5초 간격 상태 조회 → 완료 시 플레이어 표시) | 진행 단계 시각화 (단계별 아이콘·퍼센트 바·대기 안내) |
| **3일 (수)** | F003-B 통합 테스트 (B팀 그리드 이미지 + Shot 프롬프트 입력 시나리오 직접 실행), 결과 품질 기록 | F003-B: 영상 완료 후 Supabase 저장 (URL + 메타데이터), 영상 라이브러리 연결 완료 | 영상 라이브러리 연결 (영상 카드·인라인 재생·다운로드), F003-B 전체 플로우 통합 QA | 영상 카드·비디오 플레이어 디자인 완성 |
| **4일 (목)** | **F003-A 착수 판단** — F003-B 안정 시 Shot 세션 기획 BE·FE 공유 / 불안정 시 안정화 집중 | [F003-B 안정 시] F003-A: `storyboard_sessions` · `storyboard_shots` 테이블 생성, `/api/storyboard-session` 구현 시작 | [F003-B 안정 시] F003-A: StoryboardEditor 페이지 라우팅 + ShotCard 컴포넌트 설계 | [F003-B 안정 시] Shot 세션 에디터 UI 디자인 (ShotCard 상태별 화면) |
| **5일 (금)** | 3주차 전체 점검 (F003-B 완료 확인), 4주차 플랜 확정 (F003-A 주력 or F004 도전 여부) | F003-A 진행 중이면 Shot 세션 생성 + 개별 polling 연결 / F004 선택 시 Claude 역프롬프트 API 시작 | F003-A 진행 중이면 ShotCard 상태 UI 완성 / F004 선택 시 역프롬프트 탭 착수 | 전체 흐름 디자인 일관성 점검 |

**3주차 완료 기준**
- [ ] F003-B: Shot 프롬프트 + 그리드 이미지 → 영상 1개 생성·저장·재생 전체 플로우 동작 ← **B등급 달성**
- [ ] 영상 라이브러리 저장·조회 동작
- [ ] 이미지+영상 통합 라이브러리 정상 작동
- [ ] (여유 시) F003-A Shot 세션 생성 + 개별 Shot polling 동작 착수

---

## 4주차 — F003-A/S 완성 · 폴리싱 · 배포

**주간 목표:** F003-A Shot 세션 완성 → (여유 시) F003-S 이어붙이기 → Railway 배포 → 최종 발표

| 일차 | PM | BE | FE | UX/UI |
|------|----|----|----|----|
| **1일 (월)** | F003-A QA 시나리오 작성 (Shot 세션 생성·재생성·상태 추적), 클라이언트 평가 포인트 점검 | F003-A 마무리: Shot 재생성 API `/api/storyboard-shot/:id/regenerate`, 세션 상태 조회 `/api/storyboard-session/:id` | F003-A: ShotCard 재생성 버튼 연결, SessionProgressBar, 페이지 새로고침 후 상태 복원 | Shot 세션 에디터 UI 완성 (재생성 상태·완료 상태·에러 상태 분기) |
| **2일 (화)** | **F003-A 완료 확인 → F004 착수 판단** / F004 착수 시 역프롬프트 4단계 흐름 BE·FE 공유 | [F003-A 완료 시] F004: `/api/reverse-prompt` 구현 (Claude Sonnet 이미지 분석·프롬프트 추출), `/api/reverse-prompt-generate` (GPT Image 2 재생성) | [F003-A 완료 시] F004: ReversePromptUpload + ExtractedPromptEditor + RegenerateButton 컴포넌트 구현 | [F003-A 완료 시] 역프롬프트 탭 UI 디자인 (업로드→추출→수정→재생성 4단계 흐름) |
| **3일 (수)** | 전체 QA 주도 (F001·F003-B·F003-A·F003-S 시나리오 기준 실행, 버그 우선순위 분류) | F003-S 완료 또는 안정화, 라이브러리 필터·다운로드 최종 확인, 비용 한도 재확인 | 반응형 레이아웃·UI 폴리싱·마이크로카피, 버그 수정 | 비주얼 폴리싱, 전체 화면 일관성 최종 점검 |
| **4일 (금)** | GitHub README 작성 (서비스 소개·기술 스택·팀원·실행 방법), 발표 리허설 | **Railway 배포 완료** (PD 계정 환경변수 설정·API 키 주입), 배포 URL 공유, 핫픽스 대기 | Railway 배포 점검, 빌드 에러 수정, 배포 URL 전체 기능 최종 확인 | 데모 영상 녹화 (3분 이내), 발표 자료 화면 캡처·삽입 |
| **5일 (토/마감)** | **최종 발표·시연·회고** | 배포 안정화·핫픽스 대기 | 배포 안정화·핫픽스 대기 | 발표 자료 최종본 제출 |

**4주차 최종 제출물**
- [ ] 배포 데모 URL (Railway)
- [ ] GitHub Public Repo
- [ ] 3분 시연 영상
- [ ] README

---

## 기능별 담당 API 정리

| 등급 | 기능 | API Route | 담당 | 사용 기술 |
|------|------|-----------|------|---------|
| 필수 | F001 이미지 생성 | `POST /api/generate-image` | BE | GPT Image 2 / Gemini Flash |
| 필수 | F005 라이브러리 저장 | `POST /api/library` | BE | Supabase Storage + DB |
| 필수 | F005 라이브러리 조회 | `GET /api/library` | BE | Supabase DB |
| **B** | F003-B 영상 생성 | `POST /api/generate-video` | BE | fal.ai Kling 3.0 / Seedance 2.0 |
| **B** | F003-B 상태 조회 | `GET /api/video-status` | BE | fal.ai queue status |
| **A** | F003-A 세션 생성 + 일괄 생성 | `POST /api/storyboard-session` | BE | fal.ai + Supabase |
| **A** | F003-A Shot 재생성 | `POST /api/storyboard-shot/:id/regenerate` | BE | fal.ai + Supabase |
| **A** | F003-A 세션 상태 조회 | `GET /api/storyboard-session/:id` | BE | Supabase |
| **S** | F004 역프롬프트 추출 | `POST /api/reverse-prompt` | BE | Claude Sonnet (멀티모달) |
| **S** | F004 프롬프트 재생성 | `POST /api/reverse-prompt-generate` | BE | GPT Image 2 |

---

## 비용 모니터링 규칙

| 항목 | 규칙 |
|------|------|
| 영상 테스트 | 반드시 **Kling 3.0 · 5초** 사용 (Seedance는 최종 데모만) |
| 이미지 테스트 | **GPT Image 2 단일 모델** 사용 (quality: medium 고정, 데모 시 high) |
| fal.ai Hard Limit | **$120** — 키 발급 완료 즉시 설정 필수 (지연 불가) |
| 주간 비용 점검 | 매주 금요일 PM이 대시보드 확인 후 팀 공유 |

---

## 주차별 리스크 체크포인트

| 시점 | 확인 항목 | 대응 |
|------|---------|------|
| **1주차 수~금** | API 키 도착 여부 (GPT Image 2·fal.ai·Anthropic) | 미도착 시 2주차 월요일로 실제 호출 순연, 1주차는 구조·mock 완성에 집중 |
| **1주차 금** | fal.ai Hard Limit $120 설정 완료 여부 | 키 도착 전이라도 발급 즉시 Hard Limit 설정이 Day 1 필수 — 미설정 시 영상 API 테스트 전면 금지 |
| **2주차 금 (중간점검)** | 이미지 생성 + 라이브러리 저장 동작 여부 | 미통과 시 3주차에 F001·F005 우선 완성, 영상은 후순위 |
| **3주차 목** | F003-B 단일 Shot 생성 플로우 안정성 | 불안정하면 F003-A 착수 포기, B등급 완성에 집중 |
| **3주차 금** | F003-A 착수 여부 및 Shot 세션 API 구현 진행도 | 미착수 or 진도 부족 시 4주차 전반부를 F003-A에 집중 투입 |
| **4주차 화** | F003-A 완료 여부 (Shot 재생성 + 세션 상태 추적) | 미완료 시 F004 착수 포기, A등급 완성에 집중 |
| **4주차 화~수** | F004 착수 시 역프롬프트 추출 → 수정 → 재생성 흐름 완성 여부 | 시간 부족 시 1~2단계(추출·수정)만 완성하고 배포 집중 |
| **4주차 수** | Railway 배포 URL 정상 동작 | 배포 실패 시 목요일 오전까지 핫픽스 마감 |
