---
title: BE 배포 인수인계 공유 사항 (초안)
author: lsg (kx-backend 소스코드 확인 기준 — BE 담당자 확인 전)
category: 06_참고자료
status: draft
created: 2026-08-02
updated: 2026-08-02
tags:
  - 배포
  - AWS
  - 인프라
  - 인수인계
summary_target: true
---
# BE 배포 인수인계 공유 사항 (초안)

> 작성일: 2026-08-02 | **BE 담당자 확인 전 —** **`kx-backend`** **GitHub 저장소(dev 브랜치) 소스코드만으로 확인 가능한 내용을 정리한 초안**
> FE 문서(`2026-08-02_FE_배포_인수인계_공유사항.md`)와 짝을 이루는 문서. BE 담당자 연락이 닿으면 "확인 필요" 표시된 항목부터 검증받아야 한다.
> ⚠ **현재 AWS 리소스(EC2·S3)는 담당자 개인 AWS 계정에 연결돼 있고, 추후 회사 AWS 계정으로 이전 예정** — 아래 내용은 이 전제를 반영해 작성했다. 구조도는 `2026-08-02_BE_인프라_구조도.md` 참고.
> 관련 문서: `01_기획/2026-08-02_기능명세서_v8.0.md` 1장(서비스 개요)

***

## 1. 현재 배포 구조 (소스코드 확인됨)

| 항목           | 내용                                                                                                                                                                                                              |
| ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AWS 계정 | **현재 담당자 개인 AWS 계정에 EC2·S3가 연결돼 있음 — 추후 회사 AWS 계정으로 이전 예정** (담당 PM 확인). 아래 항목들은 계정 전환 시 대부분 새로 발급/재구축이 필요하다 |
| 레포           | `KX-project-A2/kx-backend` — `dev`/`main` 브랜치 푸시 시 GitHub Actions 자동 배포                                                                                                                                         |
| 배포 방식        | GitHub Actions(`.github/workflows/deploy.yml`) → Docker 이미지 빌드 → **Docker Hub**에 푸시 → EC2로 SCP 전송 → EC2에서 SSH로 `docker compose pull && up -d` 실행                                                                |
| 실행 환경        | EC2 인스턴스 1대 위에서 **nginx + Spring Boot 앱 + MySQL**이 모두 컨테이너로 함께 실행(`docker-compose.aws.yml`) — 별도 로드밸런서·컨테이너 오케스트레이션(ECS/EKS) 없이 단일 서버                                                                           |
| 현재 도메인       | `kx-studio.duckdns.org` — DuckDNS 동적 DNS(임시), FE 문서에서도 동일하게 확인됨                                                                                                                                                 |
| DB           | **AWS RDS가 아니라 EC2 위 Docker 컨테이너로 직접 띄운 MySQL 8.4** — 컨테이너 볼륨(`kx_mysql_data`)에 데이터 저장. 관리형 DB 서비스가 아님                                                                                                          |
| DB 스키마 관리    | Flyway/Liquibase 같은 버전 관리형 마이그레이션 도구 없음 — JPA `ddl-auto: update`로 엔티티 변경 시 자동 반영되는 방식                                                                                                                           |
| 이미지 저장       | **AWS S3** (담당 PM 확인 — EC2+S3 구조 맞음). 코드상으로는 로컬/S3 두 방식을 다 지원하고 `STORAGE_TYPE` 환경변수로 전환하는 구조라, 운영 서버 `.env`에 `STORAGE_TYPE=s3`+`AWS_S3_BUCKET` 등이 설정돼 있을 것으로 판단됨(값 자체는 코드에 없어 BE 확인 시 버킷명·리전만 재확인하면 됨, 2.1절 참고) |
| SSL          | Let's Encrypt(certbot) 컨테이너로 발급. `CERTBOT_DOMAIN` 환경변수에 지정된 도메인 기준으로 발급되며, 자동 갱신 스케줄(cron 등)은 compose 파일에서 확인되지 않음                                                                                              |
| Health check | `GET /api/health` 엔드포인트 존재 — 인프라 이전 시 로드밸런서/모니터링 헬스체크로 바로 활용 가능                                                                                                                                                 |
| 이미지 레지스트리    | AWS ECR이 아니라 **Docker Hub** 사용 (`${DOCKERHUB_USERNAME}/kx-backend`)                                                                                                                                             |

***

## 2. 정식 배포 전 확인·준비 필요한 것

### 2.1 S3 데이터 이관 방식 확인 (BE 확인)

* 스토리지는 S3. 코드 구조상 `storage.type=s3`일 때 `AWS_S3_BUCKET`(버킷명)·`AWS_S3_REGION`(기본값 `ap-northeast-2`)·`AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY` 값으로 연결하도록 돼 있다(`StorageConfig.java`, `StorageProperties.java`).
* **이 버킷도 개인 AWS 계정 소유일 가능성이 높다.** 회사 계정으로 전환하면 "같은 버킷을 교차 계정 권한으로 그대로 쓸지" 또는 "회사 계정에 새 버킷을 만들고 기존 데이터(이미지·영상 원본)를 복사해올지"를 정해야 한다 — 이건 인프라 구조를 결정하는 문제라 BE/인프라 담당이 판단할 사안이고, 우리는 판단에 필요한 정보(버킷명·리전·대략적 용량)만 확인해두면 된다.

### 2.2 DB 데이터 이관 방식 확인

* 현재는 EC2 위 컨테이너 MySQL이라 DB가 EC2 인스턴스와 생명주기를 같이한다. **회사 계정으로 넘어가면 새 EC2(또는 다른 방식)에 DB를 새로 띄우게 될 텐데, 그 시점에 지금 쌓인 데이터를 그대로 옮길지 결정이 필요하다.**
* 백업 정책(스냅샷 등)이 현재 compose 설정에는 보이지 않는다 — 별도로 있는지, 계정 전환 시 어떤 방식으로 데이터를 이관할 계획인지는 BE/인프라 담당이 판단할 사안이다.

### 2.3 필요한 환경변수/시크릿 목록 (코드에서 이름만 확인됨, 값은 BE만 보유)

이전 시 새 서버/CI에 아래 값들을 다시 세팅해야 한다. 이 중 `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY`·`EC2_HOST`/`EC2_SSH_KEY`처럼 AWS 계정에 직접 묶인 값들은 회사 계정 전환 시 기존 값을 재사용할 수 없고 **전부 새로 발급**받아야 한다.

| 구분                         | 변수명                                                                                                                       |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| DB                         | `DB_URL`, `DB_USERNAME`, `DB_PASSWORD` (또는 compose용 `MYSQL_DATABASE`/`MYSQL_USER`/`MYSQL_PASSWORD`/`MYSQL_ROOT_PASSWORD`) |
| 인증                         | `JWT_SECRET`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `OAUTH2_REDIRECT_URI`                                           |
| AI 연동                      | `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `FAL_KEY`, `FAL_WEBHOOK_URL`                                                       |
| 스토리지(S3 사용 시)              | `AWS_S3_BUCKET`, `AWS_S3_REGION`, `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`                                            |
| 도메인/SSL                    | `CERTBOT_DOMAIN`, `CERTBOT_EMAIL`                                                                                         |
| 쿠키/CORS                    | `AUTH_COOKIE_SECURE`, `AUTH_COOKIE_SAME_SITE`, `CORS_ALLOWED_ORIGINS`                                                     |
| 배포(GitHub Actions Secrets) | `DOCKERHUB_USERNAME`, `DOCKERHUB_TOKEN`, `EC2_HOST`, `EC2_USERNAME`, `EC2_SSH_KEY`, `EC2_APP_DIR`                         |

### 2.4 Custom Domain 연결 여부 (FE와 동일한 이슈)

* 지금은 `kx-studio.duckdns.org`(DuckDNS 임시 동적 DNS) 사용 중
* 정식 도메인으로 옮기면 `CERTBOT_DOMAIN` 값 교체 + FE의 `VITE_API_BASE_URL`도 함께 교체해야 한다(FE 문서 2.2절과 연결되는 부분)

***

## 3. 결론

**BE 담당자와 연락 없이 저장소 코드만으로 확인 가능한 배포 구조는 위와 같이 정리했다.** "개인 AWS 계정 → 회사 계정 이전 예정"이라는 전제를 놓고 다시 보면, 애초에 확인이 필요했던 항목의 성격이 달라진다 — **지금 값을 알아내서 그대로 옮기는 문제가 아니라, 계정을 통째로 바꾸면서 무엇을 새로 만들고 무엇을 이어받을지 판단하는 문제다.** 그래서 BE 확인이 필요한 항목을 아래와 같이 다시 정리한다:

1. **회사 AWS 계정 전환 일정** — 언제로 예정돼 있는지(PM 입장에서 다른 인수인계 타임라인과 맞춰볼 정보로 필요)
2. **S3 데이터(이미지·영상) 이관 방식** — 버킷을 교차 계정으로 그대로 쓸지, 새로 만들고 복사할지는 BE/인프라가 결정할 사안이지만, 결정 이전에 현재 버킷명·리전·대략적인 데이터 용량 정도는 파악해두는 게 좋다 (2.1절)
3. **DB 데이터 이관 방식** — 마찬가지로 결정은 BE 몫이지만, 지금 쌓인 데이터를 버릴지 옮길지 자체는 사전에 확인해둘 필요가 있다 (2.2절)
4. **환경변수 재발급 계획** — AWS 계정에 묶인 값들(IAM 키, EC2 관련 GitHub Actions Secrets)은 계정 전환 시 100% 새로 발급돼야 하므로, 그 작업이 언제·누구 담당으로 진행되는지 (2.3절)

반대로 **RDS 전환 여부, 버킷을 그대로 쓸지 새로 만들지 같은 아키텍처 결정 자체는 BE/인프라 담당이 판단할 몫이라 이 문서에서 요구하지 않는다.** PM 쪽에서 필요한 건 "언제, 무엇이 바뀌는지"를 미리 파악해 다른 일정과 조율하는 것이다.

BE 담당자 연락이 닿으면 위 4가지부터 확인받아 문서를 최종본으로 전환하는 걸 권장한다.
